# All build artifacts (server obj/bin/publish via Directory.Build.props, client via vite outDir)
/build/

# IDE
.vs/
*.user

# Client dependencies
node_modules/

# Secrets and local runtime data
userpass.txt
data/
